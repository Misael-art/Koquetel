# PT-01 R2 canonical bundle

Verdict: **PARTIAL**. The tmpfs main arm and both fork/OFD variants passed under
100-process contention; the mandatory ext4/XFS arm is **BLOCKED** because this
host exposes tmpfs at `/tmp` and btrfs at the workspace, with no accessible
ext4/XFS target that could be used without host mutation.

The complete 10,000-row main log is `acquisition-log.csv`; the two 10,000-row
fork variants are combined in `fork-variants.csv`. `overlap-report.json`,
`fork-a-overlap-report.json`, and `fork-b-overlap-report.json` are generated by
the retained detector. `pass-fail.json` is generated from those reports and
`filesystem-probe.txt`, not authored independently.

This is a new run beside the immutable prior bundle. Verify every retained file
with `sha256sum -c SHA256SUMS`.
