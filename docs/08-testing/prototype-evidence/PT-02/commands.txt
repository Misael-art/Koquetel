python3 harness.py truncation-matrix.csv   # summary JSON -> stdout; full matrix -> csv
# n=1000 entries (seq + chained entryDigest); truncate the final line at every offset
# 0..len-1; recover; run twice for idempotency; plus one mid-file digest-flip case.
